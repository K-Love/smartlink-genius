# .github/workflows/api-tests.yml
name: API Tests

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'src/app/api/**'
      - 'src/lib/**'
      - 'src/types/**'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'src/app/api/**'
      - 'src/lib/**'
      - 'src/types/**'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Type check
      run: npm run type-check # Make sure this script exists in package.json
    
    - name: Run tests
      run: npm test -- --coverage # Make sure you have tests configured
    
    - name: Lint
      run: npm run lint